<nav class="navbar navbar-light bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand text-white">ADMIN</a>

    <div class="d-flex">
      <% if(products) { %>
      <!-- <form method="GET" action="/admin/products/list" class="d-flex me-2">
        <input
          class="form-control me-2"
          type="search"
          placeholder="Tìm kiếm"
          aria-label="Search"
          name="searchValue"
        />
        <button class="btn btn-outline-success" type="submit">Tìm</button>
      </form> -->
      <% } %> <% if(users) { %>
      <form method="GET" action="/admin/user/list" class="d-flex me-2">
        <input
          class="form-control me-2"
          type="search"
          placeholder="Tìm kiếm"
          aria-label="Search"
          name="searchValue"
        />
        <button class="btn btn-outline-success" type="submit">Tìm</button>
      </form>
      <% } %>
      <% if(promotion) { %>
        <form method="GET" action="/admin/promotion/list" class="d-flex me-2">
          <input
            class="form-control me-2"
            type="search"
            placeholder="Tìm kiếm"
            aria-label="Search"
            name="searchValue"
          />
          <button class="btn btn-outline-success" type="submit">Tìm</button>
        </form>
      <% } %>
      <a href="/auth/logout" class="btn btn-outline-secondary">
        <i class="bi bi-box-arrow-in-right"></i> &nbsp;Đăng xuất
      </a>
    </div>
  </div>
</nav>
